<Window x:Class="TallerApp.Views.LoginWindow"
        Title="Login - Taller App" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Usuario:" Margin="10"/>
        <TextBox Grid.Row="1" Text="{Binding Usuario}" Margin="10"/>

        <TextBlock Grid.Row="2" Text="Contraseña:" Margin="10"/>
        <PasswordBox Grid.Row="3" Name="PasswordBox" Margin="10"/>

        <Button Grid.Row="4" Content="Iniciar Sesión" 
                Command="{Binding LoginCommand}" Margin="10"/>
    </Grid>
</Window>